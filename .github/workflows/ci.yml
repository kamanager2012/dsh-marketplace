name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Point contributors at dsh-community
        run: |
          echo "This repository is a redirect."
          echo "Development lives in https://github.com/kamanager2012/dsh-community/tree/main/packages/marketplace"
